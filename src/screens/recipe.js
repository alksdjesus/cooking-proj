import React from "react";
import Meal from "../components/item"


export default function Recipe({ mealData}) {

      return (
  
        <main>
            <li>Hello</li>
          {/* <section className="meals">
            {mealData.results.map((meal) => {
              return <Meal key={meal.id} meal={meal} />;
            })}
          </section> */}
        </main>
      );
    }